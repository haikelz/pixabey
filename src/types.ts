export type layout = {
    children: any,
    title: string,
}

export type splitAtoms = {
    src: string,
    hearts: string,
    comments: string,
    views: string,
    user: string,
    tags: string,
    visible: any,
    showMoreItems: any,
}
